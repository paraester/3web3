<IfModule mod_rewrite.c>
    RewriteEngine On

    # Se não for para a pasta pública, executa o index.php
    RewriteCond %{REQUEST_FILENAME} !11_chat_loginNovo/Public/
    RewriteRule ^ index.php
</IfModule>
