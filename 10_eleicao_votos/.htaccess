<IfModule mod_rewrite.c>
    RewriteEngine On

    # Se não for para a pasta pública, executa o index.php
    RewriteCond %{REQUEST_FILENAME} !10_eleicao_votos/Public/
    RewriteRule ^ index.php
</IfModule>
